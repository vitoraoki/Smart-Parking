<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartpark">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/smart_campus"
        android:label="@string/app_name"
        android:roundIcon="@drawable/smart_campus"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".Views.MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Views.SingleEvent"
            android:parentActivityName=".Views.MainActivity"/>

        <activity android:name=".Views.RepetitiveEvent"
            android:parentActivityName=".Views.MainActivity"/>

        <activity android:name=".Views.ListEvents"
            android:parentActivityName=".Views.MainActivity"
            android:launchMode="singleTop"/>

        <activity android:name=".Views.NearInstitutesParkingLots"
            android:parentActivityName=".Views.ListEvents"
            android:launchMode="singleTop"/>

        <activity android:name=".Views.MapsActivity"
            android:parentActivityName=".Views.NearInstitutesParkingLots"/>

    </application>

</manifest>