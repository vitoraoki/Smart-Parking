<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartparking">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/smart_campus"
        android:label="@string/app_name"
        android:roundIcon="@drawable/smart_campus"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="com.example.smartparking.Views.MainActivity"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.smartparking.Views.SingleEvent"
            android:parentActivityName="com.example.smartparking.Views.MainActivity" />
        <activity
            android:name="com.example.smartparking.Views.RepetitiveEvent"
            android:parentActivityName="com.example.smartparking.Views.MainActivity" />
        <activity
            android:name="com.example.smartparking.Views.ListEvents"
            android:launchMode="singleTop"
            android:parentActivityName="com.example.smartparking.Views.MainActivity" />
        <activity
            android:name="com.example.smartparking.Views.ConsultInstitute"
            android:launchMode="singleTop"
            android:parentActivityName="com.example.smartparking.Views.MainActivity"/>
        <activity
            android:name="com.example.smartparking.Views.NearInstitutesParkingLots"
            android:launchMode="singleTop"
            android:parentActivityName="com.example.smartparking.Views.ListEvents" />
        <activity android:name="com.example.smartparking.Views.MapsActivity" />
    </application>

</manifest>